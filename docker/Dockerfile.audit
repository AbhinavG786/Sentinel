FROM node:20-alpine

WORKDIR /app

# Copy full monorepo
COPY . .

# Install deps only for this service
RUN npm install --workspace=src/services/audit-service

# Build
RUN npm run build --workspace=src/services/audit-service

EXPOSE 4003

CMD ["node", "src/services/audit-service/dist/server.js"]
